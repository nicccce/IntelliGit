# Views — 业务视图

此目录存放按业务场景划分的页面级视图组件。

## 规划视图

| 视图 | 说明 |
|---|---|
| `Workspace/` | 工作区视图（文件状态、暂存、提交） |
| `History/` | 历史拓扑图（Git Log 可视化） |
| `Settings/` | 应用设置页面 |
| `BranchGraph/` | 分支合并拓扑图 |

每个视图应为独立文件夹，包含 `index.tsx` 和局部样式。
